[project]
name = "qrisp-optimization"
version = "0.1.0"
description = "A collection of problems and algorithms for eclipse/qrisp for combinatorial optimization"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "jupyter>=1.1.1",
    "qrisp>=0.9.8",
]
